describe('Pruebas en demoComponent', () => {
    
    test('Esta prueba no debe de fallar', () => { 

        const mensaje1="HolaMundo"
        const mensaje2=mensaje1.trim()
         expect(mensaje1).toBe(mensaje2)
     })
 })

 /*
 inicializacikon
 estimulo
 observer
 */